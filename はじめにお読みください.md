# カメラAI×写真オンラインスクール ホームページ 使い方ガイド

## ファイル構成

```
camera-ai-school-hp/
├── index.html              … ホームページ本体（基本さわらなくてOK）
├── data.js                 … ★掲載内容はここを編集します★
├── photos/                 … 写真を入れるフォルダ
└── はじめにお読みください.md … このファイル
```

## ホームページを見る

`index.html` をダブルクリックすると、ブラウザで開きます。

## 内容を更新する（data.js の編集）

`data.js` をテキストエディタ（メモ帳・CotEditorなど）で開いて編集します。
編集して保存したら、ブラウザで再読み込みすると反映されます。

### 対談動画の上の文章を書き換える

`data.js` のいちばん上にある `TALK_INTRO` の、
バッククォート（`）で囲まれた部分を自由に書き換えてください。
1行空けると段落の区切りになります。

### 対談動画を差し替える・増やす

`TALK_VIDEOS` の `url` を実際のYouTube URLに書き換えて、
`thumb:` の行を削除してください（サムネイルは自動で表示されます）。
同じ形で `{ ... },` を追加すれば、動画を増やせます。

```js
{
  title: "動画のタイトル",
  desc: "ひとこと説明",
  url: "https://www.youtube.com/watch?v=XXXXXXXXXXX",
},
```

### 受講生の変化（事例）を追加する

`data.js` の `STORIES` に `{ ... },` のかたまりを追加すると、
Before→Afterの事例カードが増えていきます。

```js
{
  name: "Aさん",
  role: "属性や受講期間（省略OK）",
  before: "受講前のエピソード",
  after: "受講後の変化",
  voice: "「本人のひとこと（省略OK）」",
},
```

### 写真を実際のものに差し替える

1. `photos` フォルダに写真を入れる（例：`hero01.jpg`、`offkai01.jpg`）
2. `data.js` の `HERO_PHOTOS`（トップのコラージュ）と
   `OFFKAI_PHOTOS`（オフ会ギャラリー）を書き換える：

```js
const HERO_PHOTOS = [
  "photos/hero01.jpg",
  "photos/hero02.jpg",
  // …18枚くらいがきれいに並びます
];
```

※ 写真は横幅1000px程度に縮小しておくと表示が速くなります。

## インターネットに公開する

このフォルダをそのままアップロードすれば公開できます。かんたんな順に：

1. **Netlify Drop**（無料・登録あり）
   https://app.netlify.com/drop にフォルダをドラッグ&ドロップするだけ
2. **GitHub Pages**（無料）… GitHubにフォルダを置いて公開設定
3. **レンタルサーバー** … お持ちのサーバーにFTPでアップロード

公開作業もお手伝いできますので、お気軽にご相談ください。

### 公開したら1つだけ確認

LINEやSNSでURLをシェアしたときに出るプレビュー画像・文章は、
`index.html` の上の方にある「OGP」と書かれた部分で設定しています。
公開後のURLと、プレビューに出したい写真（横1200×縦630px推奨）への
差し替えが必要です。ここはこちらで対応しますのでご相談ください。
